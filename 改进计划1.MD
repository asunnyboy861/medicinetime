# 📋 MedCabinet 软件改进计划 V1.1

> **文档版本**: V1.1  
> **创建日期**: 2026年3月14日  
> **更新日期**: 2026年3月14日  
> **基于分析**: 用户使用流程、数据流、代码质量审计、现有代码深度审查  
> **预计完成周期**: 1.5周

---

## 📊 一、改进总览

### 1.1 问题优先级矩阵

| 优先级 | 问题类型 | 影响范围 | 预计工时 | 状态 |
|--------|----------|----------|----------|------|
| 🔴 P0 | 删除操作Bug | 核心功能错误 | 0.5h | 待修复 |
| 🔴 P0 | 使用药物UI入口缺失 | 核心流程不完整 | 1.5h | 待实现 |
| 🟡 P1 | 条码扫描无实际处理 | 用户体验差 | 2h | 待实现 |
| 🟡 P1 | 低库存补货清单 | 功能缺失 | 1.5h | 待实现 |
| 🟢 P2 | 高级搜索筛选 | 体验优化 | 1h | 待实现 |
| 🟢 P2 | 导出功能完善 | 功能缺失 | 1.5h | 待实现 |

### 1.2 现有代码复用分析

| 功能模块 | 现有代码位置 | 复用方式 |
|----------|--------------|----------|
| 使用药物逻辑 | `NotificationManager+Actions.swift` | 直接复用 `handleUseMedication` |
| 低库存通知 | `NotificationManager+Actions.swift` | 直接复用 `scheduleLowStockNotification` |
| 通知分类 | `NotificationManager+Actions.swift` | 已配置完成 |
| MedicationUsage实体 | Core Data模型 | 已定义，直接使用 |
| 颜色扩展 | `Color+Hex.swift` | 使用 `Color.appPrimary` 等 |
| 日期扩展 | `Extensions.swift` | 使用 `addingDays()` 等 |
| 条码字段 | Medication实体 | 已有 `barcode` 属性 |

### 1.3 改进原则遵循

| 原则 | 说明 | 执行方式 |
|------|------|----------|
| 单一职责 | 一个功能一个模块 | 每个功能独立文件/扩展 |
| 高内聚低耦合 | 模块间最小依赖 | 通过ViewModel通信 |
| 代码复用 | **优先复用现有模块** | 复用现有服务方法 |
| 最小代码 | 避免重复代码 | 不重复实现已有功能 |
| 风格一致 | UI/UX风格统一 | 复用现有Color/Font扩展 |
| 继承优先 | 利用现有架构 | 扩展现有ViewModel |

---

## 🔴 二、P0 严重问题修复

### 2.1 修复删除操作Bug

#### 需求描述
当前 `MedicationListView.swift` 中的删除操作存在逻辑错误，删除后错误地调用了更新方法。

#### 当前代码问题
```swift
// 文件: Views/MedicationListView.swift (第63行)
// ❌ 错误代码
private func deleteMedications(at offsets: IndexSet) {
    for index in offsets {
        let medication = viewModel.filteredMedications[index]
        viewContext.delete(medication)
    }
    viewModel.updateMedication(viewModel.filteredMedications.first!) // 错误：更新了错误的药物
}
```

#### 修复方案
```swift
// 文件: Views/MedicationListView.swift
// ✅ 正确代码 - 复用现有ViewModel方法
private func deleteMedications(at offsets: IndexSet) {
    for index in offsets {
        let medication = viewModel.filteredMedications[index]
        viewModel.deleteMedication(medication) // 使用ViewModel统一方法
    }
}
```

#### 测试用例
```swift
// 文件: Tests/MedicationListViewTests.swift (新建)
import XCTest
import CoreData
@testable import medicinetime

final class MedicationListViewTests: XCTestCase {
    var viewModel: MedicationViewModel!
    var persistenceController: PersistenceController!
    
    override func setUp() {
        persistenceController = PersistenceController(inMemory: true)
        viewModel = MedicationViewModel(persistenceController: persistenceController)
    }
    
    func testDeleteMedicationRemovesFromList() async {
        // Given: 创建测试药物
        let medication = Medication(context: persistenceController.container.viewContext)
        medication.id = UUID()
        medication.name = "Test Med"
        medication.quantity = 10
        persistenceController.saveContext()
        
        let initialCount = viewModel.medications.count
        
        // When: 删除药物
        viewModel.deleteMedication(medication)
        
        // Then: 验证删除成功
        await Task.yield()
        XCTAssertEqual(viewModel.medications.count, initialCount - 1, "药物应该被删除")
        XCTAssertFalse(viewModel.medications.contains { $0.id == medication.id }, "已删除的药物不应存在")
    }
}
```

#### 检验标准
- [ ] 删除单个药物后，列表数量减少1
- [ ] 删除后不会触发其他药物的更新
- [ ] 删除后Core Data持久化正确

---

### 2.2 添加"使用药物"快捷功能

#### 需求描述
用户需要快速记录用药行为，自动减少库存数量。
**重要发现**：核心逻辑已在 `NotificationManager+Actions.swift` 中实现，只需添加UI入口。

#### 现有代码分析

**已实现的功能** (`NotificationManager+Actions.swift`):
```swift
// 已有方法 - 可直接复用
private func handleUseMedication(medicationID: UUID) async {
    // 1. 获取药物
    // 2. 减少库存
    // 3. 更新 lastUsedDate
    // 4. 检查低库存
    // 5. 保存
}

// 已有方法 - 可直接复用
private func scheduleLowStockNotification(for medication: Medication) {
    // 发送低库存通知
}
```

**Core Data模型已定义** (`MedicationUsage` 实体):
```xml
<entity name="MedicationUsage" representedClassName="MedicationUsage" syncable="YES">
    <attribute name="actionType" optional="YES" attributeType="String"/>
    <attribute name="date" optional="YES" attributeType="Date"/>
    <attribute name="id" optional="YES" attributeType="UUID"/>
    <attribute name="medicationID" optional="YES" attributeType="UUID"/>
    <attribute name="notes" optional="YES" attributeType="String"/>
    <attribute name="quantity" attributeType="Integer 16"/>
    <attribute name="usedBy" optional="YES" attributeType="String"/>
    <relationship name="medication" destinationEntity="Medication"/>
</entity>
```

#### 优化方案 - 复用现有代码

**方案选择**：将现有 `handleUseMedication` 重构为公共方法，供UI层调用。

**1. 扩展 NotificationManager** (修改现有文件):
```swift
// 文件: Services/NotificationManager+Actions.swift
// 将 private 方法改为 public，供UI层调用

/// 记录药物使用（公开方法，供UI层调用）
/// - Parameters:
///   - medication: 使用的药物
///   - quantity: 使用数量
///   - notes: 备注（可选）
public func recordMedicationUsage(
    medication: Medication,
    quantity: Int16,
    notes: String? = nil
) async {
    let context = PersistenceController.shared.container.viewContext
    
    await MainActor.run {
        // 1. 更新库存
        medication.quantity = max(0, medication.quantity - quantity)
        medication.lastUsedDate = Date()
        medication.lastUpdated = Date()
        
        // 2. 检查低库存
        if medication.quantity <= medication.lowStockThreshold {
            medication.isLowStock = true
        }
        
        // 3. 创建使用记录（复用现有MedicationUsage实体）
        let usage = MedicationUsage(context: context)
        usage.id = UUID()
        usage.medicationID = medication.id
        usage.date = Date()
        usage.quantity = quantity
        usage.notes = notes
        usage.actionType = "use"
        usage.medication = medication
        
        // 4. 保存
        do {
            try context.save()
            
            // 5. 发送低库存通知（复用现有方法）
            if medication.needsRestock {
                scheduleLowStockNotification(for: medication)
            }
        } catch {
            print("Error recording usage: \(error)")
        }
    }
}
```

**2. ViewModel扩展** (修改现有文件):
```swift
// 文件: ViewModels/MedicationViewModel.swift
// 添加使用药物方法

extension MedicationViewModel {
    
    /// 使用药物 - 调用NotificationManager的公共方法
    /// - Parameters:
    ///   - medication: 使用的药物
    ///   - quantity: 使用数量
    ///   - notes: 备注（可选）
    func useMedication(_ medication: Medication, quantity: Int16, notes: String? = nil) {
        Task {
            await NotificationManager.shared.recordMedicationUsage(
                medication: medication,
                quantity: quantity,
                notes: notes
            )
        }
    }
}
```

**3. 使用药物弹窗组件** (新建文件):
```swift
// 文件: Views/Components/UseMedicationSheet.swift
// 职责：提供使用药物的UI交互

import SwiftUI

struct UseMedicationSheet: View {
    @Environment(\.dismiss) private var dismiss
    @EnvironmentObject private var viewModel: MedicationViewModel
    
    let medication: Medication
    
    @State private var quantityToRemove: Int16 = 1
    @State private var notes: String = ""
    
    // 最大可使用数量
    private var maxUsable: Int16 {
        max(0, medication.quantity)
    }
    
    var body: some View {
        NavigationView {
            Form {
                // 药物信息
                Section {
                    HStack {
                        VStack(alignment: .leading, spacing: 4) {
                            Text(medication.displayName)
                                .font(.headline)
                            Text("Current: \(medication.quantity) \(medication.displayUnit)")
                                .font(.subheadline)
                                .foregroundColor(.secondary)
                        }
                        Spacer()
                        Circle()
                            .fill(medication.expiryStatus.color)
                            .frame(width: 12, height: 12)
                    }
                }
                
                // 使用数量
                Section("Quantity to Use") {
                    HStack {
                        Text("Amount")
                        Spacer()
                        Stepper(value: $quantityToRemove, in: 1...max(maxUsable, 1)) {
                            Text("\(quantityToRemove) \(medication.displayUnit)")
                                .fontWeight(.medium)
                        }
                    }
                    
                    if quantityToRemove > medication.quantity {
                        HStack {
                            Image(systemName: "exclamationmark.triangle.fill")
                                .foregroundColor(.appWarning)
                            Text("This will result in negative stock")
                                .font(.caption)
                                .foregroundColor(.appWarning)
                        }
                    }
                }
                
                // 备注
                Section("Notes (Optional)") {
                    TextField("Reason for use", text: $notes, axis: .vertical)
                        .lineLimit(2...4)
                }
                
                // 使用后预览
                Section("After Use") {
                    HStack {
                        Text("Remaining")
                        Spacer()
                        Text("\(medication.quantity - quantityToRemove) \(medication.displayUnit)")
                            .fontWeight(.medium)
                            .foregroundColor(quantityToRemove > medication.quantity ? .appError : .primary)
                    }
                    
                    if medication.quantity - quantityToRemove <= medication.lowStockThreshold {
                        Label("Will trigger low stock alert", systemImage: "exclamationmark.triangle.fill")
                            .foregroundColor(.appWarning)
                            .font(.caption)
                    }
                }
            }
            .navigationTitle("Use Medication")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .navigationBarLeading) {
                    Button("Cancel") {
                        dismiss()
                    }
                }
                
                ToolbarItem(placement: .navigationBarTrailing) {
                    Button("Confirm") {
                        confirmUsage()
                    }
                    .fontWeight(.semibold)
                    .disabled(quantityToRemove <= 0)
                }
            }
        }
    }
    
    private func confirmUsage() {
        viewModel.useMedication(
            medication,
            quantity: quantityToRemove,
            notes: notes.isEmpty ? nil : notes
        )
        dismiss()
    }
}

// MARK: - Preview
struct UseMedicationSheet_Previews: PreviewProvider {
    static var previews: some View {
        let medication = Medication(context: PersistenceController.preview.container.viewContext)
        medication.name = "Tylenol"
        medication.quantity = 30
        medication.unit = "tablets"
        
        return UseMedicationSheet(medication: medication)
            .environmentObject(MedicationViewModel(persistenceController: .preview))
    }
}
```

**4. 集成到详情页** (修改现有文件):
```swift
// 文件: Views/MedicationDetailView.swift
// 在现有代码中添加以下内容

// 添加状态变量
@State private var showingUseSheet = false

// 在Actions Section中添加按钮（在Edit按钮之前）
Button(action: { showingUseSheet = true }) {
    Label("Use Medication", systemImage: "minus.circle.fill")
        .frame(maxWidth: .infinity)
}
.buttonStyle(.borderedProminent)
.tint(.appPrimary)

// 添加sheet修饰符
.sheet(isPresented: $showingUseSheet) {
    UseMedicationSheet(medication: medication)
}
```

#### 测试用例
```swift
// 文件: Tests/UseMedicationTests.swift (新建)
import XCTest
import CoreData
@testable import medicinetime

final class UseMedicationTests: XCTestCase {
    var viewModel: MedicationViewModel!
    var persistenceController: PersistenceController!
    
    override func setUp() {
        persistenceController = PersistenceController(inMemory: true)
        viewModel = MedicationViewModel(persistenceController: persistenceController)
    }
    
    func testUseMedicationDecreasesQuantity() async {
        // Given: 创建测试药物
        let medication = Medication(context: persistenceController.container.viewContext)
        medication.id = UUID()
        medication.name = "Test Med"
        medication.quantity = 30
        medication.unit = "tablets"
        medication.lowStockThreshold = 5
        persistenceController.saveContext()
        
        // When: 使用5片
        viewModel.useMedication(medication, quantity: 5)
        
        // Then: 验证库存减少
        try? await Task.sleep(nanoseconds: 500_000_000) // 等待异步完成
        XCTAssertEqual(medication.quantity, 25, "库存应该减少5")
    }
    
    func testUseMedicationCreatesUsageRecord() async {
        // Given
        let medication = Medication(context: persistenceController.container.viewContext)
        medication.id = UUID()
        medication.name = "Test Med"
        medication.quantity = 30
        persistenceController.saveContext()
        
        // When
        viewModel.useMedication(medication, quantity: 3, notes: "Headache")
        
        // Then: 验证使用记录创建
        try? await Task.sleep(nanoseconds: 500_000_000)
        
        let request: NSFetchRequest<MedicationUsage> = MedicationUsage.fetchRequest()
        request.predicate = NSPredicate(format: "medicationID == %@", medication.id! as CVarArg)
        
        let records = try? persistenceController.container.viewContext.fetch(request)
        XCTAssertEqual(records?.count, 1, "应该创建1条使用记录")
        XCTAssertEqual(records?.first?.quantity, 3, "记录数量应为3")
        XCTAssertEqual(records?.first?.notes, "Headache", "备注应正确保存")
    }
}
```

#### 检验标准
- [ ] 使用药物后库存正确减少
- [ ] 使用记录正确创建并关联药物
- [ ] 低库存阈值触发正确
- [ ] 复用现有 `handleUseMedication` 逻辑
- [ ] 复用现有 `MedicationUsage` 实体
- [ ] UI风格与现有代码一致

---

## 🟡 三、P1 中等问题完善

### 3.1 条码扫描实际处理

#### 需求描述
扫描条码后需要自动填充药物信息。
**重要发现**：`Medication` 实体已有 `barcode` 属性，可直接使用。

#### 现有代码分析

**已有字段** (Core Data模型):
```xml
<attribute name="barcode" optional="YES" attributeType="String"/>
```

**已有扫描器** (`BarcodeScannerView.swift`):
```swift
// 已实现完整的条码扫描UI
struct BarcodeScannerView: UIViewControllerRepresentable {
    let onBarcodeScanned: (String) -> Void
    // ...
}
```

#### 优化方案

**1. 条码服务** (新建文件):
```swift
// 文件: Services/BarcodeService.swift
// 职责：条码识别和药物信息查询

import Foundation

struct BarcodeResult {
    let barcode: String
    let name: String?
    let category: String?
    let dosage: String?
    let manufacturer: String?
    let confidence: Confidence
    
    enum Confidence {
        case high    // 完全匹配
        case medium  // 部分匹配
        case low     // 未识别
    }
}

final class BarcodeService {
    static let shared = BarcodeService()
    
    private var barcodeDatabase: [String: MedicationBarcodeInfo] = [:]
    
    private init() {
        loadBarcodeDatabase()
    }
    
    private func loadBarcodeDatabase() {
        guard let url = Bundle.main.url(forResource: "MedicationBarcodes", withExtension: "json"),
              let data = try? Data(contentsOf: url) else {
            print("Warning: MedicationBarcodes.json not found, using empty database")
            return
        }
        
        do {
            let decoder = JSONDecoder()
            let barcodes = try decoder.decode([MedicationBarcodeInfo].self, from: data)
            barcodes.forEach { info in
                info.barcodes.forEach { barcode in
                    barcodeDatabase[barcode] = info
                }
            }
            print("Loaded \(barcodeDatabase.count) barcode entries")
        } catch {
            print("Error loading barcode database: \(error)")
        }
    }
    
    /// 查询条码对应的药物信息
    func lookup(barcode: String) -> BarcodeResult {
        if let info = barcodeDatabase[barcode] {
            return BarcodeResult(
                barcode: barcode,
                name: info.name,
                category: info.category,
                dosage: info.dosage,
                manufacturer: info.manufacturer,
                confidence: .high
            )
        }
        
        return BarcodeResult(
            barcode: barcode,
            name: nil,
            category: nil,
            dosage: nil,
            manufacturer: nil,
            confidence: .low
        )
    }
}

struct MedicationBarcodeInfo: Codable {
    let name: String
    let category: String
    let dosage: String?
    let manufacturer: String?
    let barcodes: [String]
}
```

**2. 本地条码数据库** (新建文件):
```json
// 文件: Resources/MedicationBarcodes.json
[
    {
        "name": "Tylenol Extra Strength",
        "category": "Pain Relief",
        "dosage": "500mg",
        "manufacturer": "Johnson & Johnson",
        "barcodes": ["300450530001", "300450530002"]
    },
    {
        "name": "Advil",
        "category": "Pain Relief",
        "dosage": "200mg",
        "manufacturer": "Pfizer",
        "barcodes": ["363730001001", "363730001002"]
    },
    {
        "name": "Benadryl",
        "category": "Allergy",
        "dosage": "25mg",
        "manufacturer": "Johnson & Johnson",
        "barcodes": ["300450540001"]
    }
]
```

**3. 集成到添加药物视图** (修改现有文件):
```swift
// 文件: Views/AddMedicationView.swift
// 添加以下代码

// 新增状态变量
@State private var scannedBarcode: String?
@State private var barcodeResult: BarcodeResult?

// 修改条码扫描sheet
.sheet(isPresented: $showingScanner) {
    BarcodeScannerView { barcode in
        handleScannedBarcode(barcode)
    }
}

// 新增条码处理方法
private func handleScannedBarcode(_ barcode: String) {
    scannedBarcode = barcode
    let result = BarcodeService.shared.lookup(barcode: barcode)
    barcodeResult = result
    
    // 保存条码到药物
    // medication.barcode = barcode (在saveMedication中处理)
    
    if result.confidence == .high {
        // 自动填充识别到的信息
        if let name = result.name {
            self.name = name
        }
        if let category = result.category {
            self.selectedCategory = viewModel.categories.first { $0.name == category }
        }
        if let dosage = result.dosage {
            // 可选：填充剂量信息
        }
    }
}

// 修改saveMedication方法，添加条码保存
private func saveMedication() {
    let medication = Medication(context: viewContext)
    medication.id = UUID()
    medication.name = name
    medication.barcode = scannedBarcode  // 添加这行
    // ... 其他字段
}
```

#### 检验标准
- [ ] 扫描已知条码自动填充信息
- [ ] 条码保存到 `barcode` 字段
- [ ] 复用现有 `BarcodeScannerView`
- [ ] 复用现有 Core Data 字段

---

### 3.2 低库存补货清单

#### 需求描述
在Dashboard中显示需要补货的药物清单。

#### 现有代码分析

**已有计算属性** (`MedicationViewModel.swift`):
```swift
var lowStockCount: Int {
    return medications.filter { $0.needsRestock }.count
}
```

**已有属性** (`Medication+Extensions.swift`):
```swift
public var needsRestock: Bool {
    return quantity <= lowStockThreshold
}
```

#### 优化方案

**1. 补货清单组件** (新建文件):
```swift
// 文件: Views/Components/RestockListView.swift
// 职责：显示和管理补货清单

import SwiftUI

struct RestockListView: View {
    @ObservedObject var viewModel: MedicationViewModel
    @State private var selectedItems: Set<UUID> = []
    
    private var restockItems: [Medication] {
        viewModel.medications.filter { $0.needsRestock }
    }
    
    var body: some View {
        NavigationView {
            List {
                if restockItems.isEmpty {
                    EmptyStateView(
                        title: "All Stocked Up!",
                        message: "No medications need restocking",
                        icon: "checkmark.circle.fill"
                    )
                } else {
                    ForEach(restockItems) { medication in
                        RestockItemRow(
                            medication: medication,
                            isSelected: selectedItems.contains(medication.id ?? UUID())
                        ) {
                            toggleSelection(medication)
                        }
                    }
                }
            }
            .navigationTitle("Restock List")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .navigationBarLeading) {
                    Button(selectedItems.count == restockItems.count ? "Deselect All" : "Select All") {
                        if selectedItems.count == restockItems.count {
                            selectedItems.removeAll()
                        } else {
                            selectedItems = Set(restockItems.compactMap { $0.id })
                        }
                    }
                }
                
                ToolbarItem(placement: .navigationBarTrailing) {
                    Button(action: { copyToClipboard() }) {
                        Image(systemName: "doc.on.clipboard")
                    }
                    .disabled(selectedItems.isEmpty)
                }
            }
        }
    }
    
    private func toggleSelection(_ medication: Medication) {
        guard let id = medication.id else { return }
        if selectedItems.contains(id) {
            selectedItems.remove(id)
        } else {
            selectedItems.insert(id)
        }
    }
    
    private func copyToClipboard() {
        let items = restockItems.filter { selectedItems.contains($0.id ?? UUID()) }
        let text = items.map { "- \($0.displayName) (\($0.quantity) \($0.displayUnit) remaining)" }
            .joined(separator: "\n")
        
        let fullText = "🛒 Restock List\n\n\(text)"
        UIPasteboard.general.string = fullText
    }
}

struct RestockItemRow: View {
    let medication: Medication
    let isSelected: Bool
    let onToggle: () -> Void
    
    var body: some View {
        Button(action: onToggle) {
            HStack(spacing: 12) {
                Image(systemName: isSelected ? "checkmark.circle.fill" : "circle")
                    .foregroundColor(isSelected ? .appPrimary : .secondary)
                
                VStack(alignment: .leading, spacing: 4) {
                    Text(medication.displayName)
                        .font(.headline)
                        .foregroundColor(.primary)
                    
                    HStack {
                        Label("\(medication.quantity) left", systemImage: "cube.box")
                            .font(.caption)
                            .foregroundColor(.appWarning)
                        
                        Text("•")
                            .foregroundColor(.secondary)
                        
                        Text("Threshold: \(medication.lowStockThreshold)")
                            .font(.caption)
                            .foregroundColor(.secondary)
                    }
                }
                
                Spacer()
            }
        }
    }
}
```

**2. 集成到Dashboard** (修改现有文件):
```swift
// 文件: Views/ContentView.swift
// 在DashboardView的Quick Actions后添加

if viewModel.lowStockCount > 0 {
    NavigationLink {
        RestockListView(viewModel: viewModel)
    } label: {
        HStack {
            Image(systemName: "cart.fill")
                .foregroundColor(.white)
            
            VStack(alignment: .leading, spacing: 2) {
                Text("Restock List")
                    .font(.subheadline)
                    .fontWeight(.medium)
                    .foregroundColor(.white)
                
                Text("\(viewModel.lowStockCount) items need restocking")
                    .font(.caption)
                    .foregroundColor(.white.opacity(0.8))
            }
            
            Spacer()
            
            Image(systemName: "chevron.right")
                .foregroundColor(.white.opacity(0.6))
        }
        .padding()
        .background(Color.appWarning)
        .cornerRadius(12)
    }
    .padding(.horizontal)
}
```

---

## 🟢 四、P2 体验优化

### 4.1 高级搜索筛选

#### 优化方案
```swift
// 文件: ViewModels/MedicationViewModel.swift
// 添加以下枚举和计算属性

enum ExpiryFilter: String, CaseIterable {
    case all = "All"
    case expired = "Expired"
    case expiringSoon = "Expiring Soon"
    case good = "Good"
}

enum StockFilter: String, CaseIterable {
    case all = "All"
    case lowStock = "Low Stock"
    case inStock = "In Stock"
}

@Published var expiryFilter: ExpiryFilter = .all
@Published var stockFilter: StockFilter = .all

// 修改现有的 filteredMedications 计算属性
var filteredMedications: [Medication] {
    var result = medications
    
    // 分类筛选（现有逻辑）
    if let category = selectedCategory {
        result = result.filter { $0.category == category.name }
    }
    
    // 过期状态筛选（新增）
    switch expiryFilter {
    case .expired:
        result = result.filter { $0.isExpired }
    case .expiringSoon:
        result = result.filter { $0.expiryStatus == .expiringSoon || $0.expiryStatus == .expiringIn3Months }
    case .good:
        result = result.filter { $0.expiryStatus == .good }
    case .all:
        break
    }
    
    // 库存状态筛选（新增）
    switch stockFilter {
    case .lowStock:
        result = result.filter { $0.needsRestock }
    case .inStock:
        result = result.filter { !$0.needsRestock }
    case .all:
        break
    }
    
    // 文字搜索（现有逻辑）
    if !searchText.isEmpty {
        result = result.filter {
            ($0.name?.localizedCaseInsensitiveContains(searchText) ?? false) ||
            ($0.notes?.localizedCaseInsensitiveContains(searchText) ?? false)
        }
    }
    
    return result
}
```

### 4.2 导出功能完善

#### 优化方案
```swift
// 文件: Services/ExportService.swift
// 职责：数据导出功能

import Foundation
import PDFKit

final class ExportService {
    static let shared = ExportService()
    
    private init() {}
    
    // MARK: - CSV导出
    
    func exportToCSV(medications: [Medication]) -> URL? {
        let csvHeader = "Name,Category,Quantity,Unit,Expiration Date,Status,Location,Notes\n"
        let csvRows = medications.map { med in
            [
                med.displayName.csvEscaped,
                med.displayCategory.csvEscaped,
                "\(med.quantity)",
                med.displayUnit.csvEscaped,
                formatDate(med.safeExpirationDate),
                med.expiryStatus.label,
                med.displayLocation.csvEscaped,
                med.displayNotes.csvEscaped
            ].joined(separator: ",")
        }
        
        let csvContent = csvHeader + csvRows.joined(separator: "\n")
        
        let url = FileManager.default.temporaryDirectory.appendingPathComponent("medications.csv")
        do {
            try csvContent.write(to: url, atomically: true, encoding: .utf8)
            return url
        } catch {
            print("Error exporting CSV: \(error)")
            return nil
        }
    }
    
    // MARK: - PDF导出
    
    func exportToPDF(medications: [Medication]) -> URL? {
        // PDF生成逻辑...
        // 使用现有的 Color.appPrimary 等颜色
    }
    
    // MARK: - 辅助方法
    
    private func formatDate(_ date: Date) -> String {
        let formatter = DateFormatter()
        formatter.dateStyle = .medium
        return formatter.string(from: date)
    }
}

// MARK: - String扩展（添加到 Extensions.swift）

extension String {
    var csvEscaped: String {
        if self.contains(",") || self.contains("\"") || self.contains("\n") {
            return "\"\(self.replacingOccurrences(of: "\"", with: "\"\""))\""
        }
        return self
    }
}
```

---

## 📁 五、文件结构总览

### 5.1 新建文件清单

| 文件路径 | 职责 | 优先级 | 说明 |
|----------|------|--------|------|
| `Views/Components/UseMedicationSheet.swift` | 使用药物弹窗 | P0 | UI组件 |
| `Services/BarcodeService.swift` | 条码识别服务 | P1 | 业务逻辑 |
| `Views/Components/RestockListView.swift` | 补货清单视图 | P1 | UI组件 |
| `Resources/MedicationBarcodes.json` | 条码数据库 | P1 | 资源文件 |
| `Services/ExportService.swift` | 数据导出服务 | P2 | 业务逻辑 |
| `Tests/UseMedicationTests.swift` | 使用药物测试 | P0 | 单元测试 |

### 5.2 修改文件清单

| 文件路径 | 修改内容 | 优先级 | 说明 |
|----------|----------|--------|------|
| `Views/MedicationListView.swift` | 修复删除Bug | P0 | 1行代码 |
| `Views/MedicationDetailView.swift` | 添加使用药物入口 | P0 | 添加按钮和sheet |
| `ViewModels/MedicationViewModel.swift` | 添加使用药物方法 | P0 | 复用现有服务 |
| `Services/NotificationManager+Actions.swift` | 添加公共方法 | P0 | 重构现有方法 |
| `Views/AddMedicationView.swift` | 集成条码处理 | P1 | 添加处理逻辑 |
| `Views/ContentView.swift` | 添加补货清单入口 | P1 | 添加导航链接 |
| `Extensions/Extensions.swift` | 添加CSV扩展 | P2 | 添加扩展方法 |

### 5.3 不需要新建的文件

| 原计划文件 | 原因 |
|------------|------|
| `Services/UsageRecordService.swift` | 复用现有 `NotificationManager+Actions.swift` |
| `Tests/MedicationListViewTests.swift` | 简化测试，合并到主测试文件 |

---

## ✅ 六、检验清单

### 6.1 功能检验

- [ ] 删除药物后列表正确更新
- [ ] 使用药物后库存正确减少
- [ ] 使用记录正确创建（复用现有实体）
- [ ] 低库存警告正确触发（复用现有方法）
- [ ] 条码扫描自动填充
- [ ] 补货清单正确显示
- [ ] 高级筛选功能正常
- [ ] CSV导出功能正常

### 6.2 代码复用检验

- [ ] 复用 `NotificationManager.handleUseMedication` 逻辑
- [ ] 复用 `NotificationManager.scheduleLowStockNotification`
- [ ] 复用 `MedicationUsage` 实体
- [ ] 复用 `Medication.barcode` 字段
- [ ] 复用 `Color.appPrimary` 等颜色扩展
- [ ] 复用 `Date.addingDays()` 等日期扩展
- [ ] 复用 `BarcodeScannerView` 扫描器

### 6.3 风格一致性检验

- [ ] UI风格与现有代码一致
- [ ] 颜色使用Color扩展
- [ ] 命名遵循现有规范
- [ ] 注释风格一致

---

## 📝 七、实施计划

### Week 1: P0严重问题修复

| 天数 | 任务 | 产出 |
|------|------|------|
| Day 1 | 修复删除Bug + 重构NotificationManager | 修复代码 + 公共方法 |
| Day 2 | 实现使用药物UI | UseMedicationSheet |
| Day 3 | 集成使用药物到详情页 + 测试 | 完整P0功能 |
| Day 4 | 条码扫描处理 | BarcodeService + JSON |
| Day 5 | 补货清单功能 | RestockListView |

### Week 2: P2功能完善

| 天数 | 任务 | 产出 |
|------|------|------|
| Day 1 | 高级筛选功能 | ViewModel筛选逻辑 |
| Day 2 | 导出功能 | ExportService |
| Day 3 | 整体测试 + Bug修复 | 测试通过 |
| Day 4 | 代码审查 + 文档更新 | 代码整洁 |
| Day 5 | 提交仓库 | 完整交付 |

---

## 🔄 八、提交规范

```
<type>(<scope>): <subject>

<body>

<footer>
```

示例：
```
fix(MedicationListView): 修复删除操作Bug

- 使用ViewModel.deleteMedication替代直接操作viewContext
- 移除错误的updateMedication调用

Closes #issue-number
```

---

## 📌 九、关键改进点总结

| 改进点 | 原计划 | 优化后 |
|--------|--------|--------|
| 使用药物服务 | 新建 `UsageRecordService` | 复用 `NotificationManager+Actions` |
| 数据模型 | 未确认 | 确认 `MedicationUsage` 已存在 |
| 条码字段 | 未确认 | 确认 `barcode` 字段已存在 |
| 低库存通知 | 新建方法 | 复用现有 `scheduleLowStockNotification` |
| 文件数量 | 8个新建 | 6个新建（减少2个） |
| 代码复用率 | 约40% | 约70% |

---

*文档版本: V1.1 | 创建日期: 2026年3月14日 | 更新日期: 2026年3月14日 | 预计完成: 2026年3月25日*
